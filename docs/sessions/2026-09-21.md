# Project Jason Session Record — 2026-09-21

## Subject

Endpoint Availability Verification and durable deferred-work behavior.

## Decision

A DRMM offline indication is evidence, not conclusive proof that an endpoint is powered off. Jason should use Last Seen to decide when secondary verification is warranted and should persist future rechecks so deferred work does not depend on technician memory.

Default behavior established:

- peer verification threshold: 2 hours offline;
- before threshold: persist a recheck for the threshold time;
- after threshold: attempt same-client/site read-only peer verification when an eligible online managed peer exists;
- intended peer checks: hostname resolution, ping by hostname, ping by last-known IP;
- peer success while DRMM is offline indicates a likely DRMM agent/service/path problem;
- failed ping is not conclusive proof of power-off;
- no suitable peer is a valid recoverable state and triggers another recheck;
- unknown Last Seen does not allow indefinite waiting.

## Implemented artifacts

- `implementation/autonomous_remediation/availability.py`
- `implementation/autonomous_remediation/test_availability.py`
- `07-Operations/Endpoint-Availability-Verification-Playbook.md`
- `implementation/autonomous_remediation/workflow.example.yaml`
- `implementation/autonomous_remediation/README.md`

Initial implementation commit:

`ce4737c7d98b3caca606b8c30f4abffb9f16ad24` — `Add durable offline endpoint verification gate`

## Persisted states

The evaluator supports explicit states including:

- `online`
- `recently_offline`
- `peer_verification_due`
- `peer_unavailable`
- `reachable_outside_drmm`
- `offline_likely`
- `inconclusive`

The persisted payload includes observation time, offline age, peer-verification requirement, recheck requirement, `next_recheck_at`, DRMM-agent suspicion, and normalized evidence.

## Governance

This work does not authorize disruptive action. Peer probing is designed as read-only/non-destructive and must remain within the same authorized client/site. Cross-client probing is prohibited.

Scheduling future work does not create authority. Any future scheduler must resume through the Central Orchestrator and revalidate principal, client, device, capability, policy, and terminal workflow state.

## Open production dependencies

1. Governed same-site peer discovery and network reachability probe capability.
2. Durable deferred-work scheduler with deduplication, restart recovery, bounded retries/aging, terminal-state cancellation, and audit.

The scheduler is tracked as `TODO-OPS-001 — Durable deferred-work recheck scheduler`.

JKD-009 remains append-only evidence storage and explicitly does not provide scheduled retries.

## Documentation propagation

This decision is reflected in the repository README, docs index, roadmap, roadmap status, capability register, capability catalog, Central Orchestrator boundary, durable event-store boundary, operational playbook, autonomous-remediation documentation, TODO backlog, and CURRENT checkpoint.


## Later-session additions captured on 2026-09-22

Work continued after the initial documentation sweep. The following state is now part of the 2026-09-21 development record:

### Datto RMM site-variable governance

- Governed site-variable reads are live through `management.site.variable.list`.
- Values are treated as potentially sensitive configuration and must not be disclosed to non-administrative requesters.
- Create/update remain inactive in the current live registry and require explicit governed activation plus readback verification.
- Delete remains intentionally unexposed by default.
- Detailed handling is documented in `07-Operations/Datto-RMM-Site-Variable-Governance.md`.

### Playbook-level autonomy

Autonomous execution is governed at the playbook/version level, not merely by a global autonomy switch. A playbook must explicitly declare autonomous eligibility and remain within approved scope. Material changes or revocation invalidate standing autonomous approval.

User-disruptive actions remain per-run approval-bound even when the surrounding playbook is approved for autonomous execution.

The canonical playbook template now contains an Autonomous Execution Eligibility section.

### MCP operational return path

The later operational work confirmed that the governed MCP return path is available in `governed-read-plus-actions` mode with `direct_provider_access=false`. This preserves the rule that provider access returns through Jason governance rather than becoming an unmanaged fallback path.

### Support-list revalidation

On 2026-09-22, the stale support backlog was rechecked against the live capability registry and original production evidence. Autotask ticket reads/notes and Datto EDR threat detail are now verified live; Datto alert resolution is now exposed as a governed action.
